import { ImagenEmptyPipe } from './imagen-empty.pipe';

describe('ImagenEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
